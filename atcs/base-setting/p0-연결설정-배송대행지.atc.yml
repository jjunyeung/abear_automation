# 연결설정 / 배송대행지 — P0 미작성 TC 7건 (auto-generated skeleton).
#
# 이 ATC 는 'scripts/generate-p0-skeletons.py' 가 자동 생성한 placeholder.
# 각 step = 엑셀 TC 1건. spec.ts 는 아직 없음 → catalog 에서 stale 로 노출됨.
#
# 작업 방법:
#   1. step.id ↔ Playwright 액션 매핑을 tests/base-setting/p0-연결설정-배송대행지.spec.ts 에 구현
#   2. tests/base-setting/p0-연결설정-배송대행지.handlerKeys.json 에 step.id 들 등록
#   3. selector 는 windly_repo/* 의 해당 컴포넌트 코드 확인 후 추출

title: 연결설정 / 배송대행지 영역 P0 TC 묶음 (7건)
priority: P0

description: |
  자동 생성된 영역 단위 skeleton. 엑셀 export 의 P0 미작성 TC 7건을 step 으로 모음.
  spec.ts 구현 전까지 catalog 에서 stale (handlerKeys 매니페스트 없음) 로 표시.

inputs: {}

steps:
  - id: tc1321_카카오톡-주문알림
    do: 배송대행지 연결 페이지 확인
  - id: tc1323_카카오톡-주문알림
    do: 퀵스타 연결 모달
  - id: tc1324_카카오톡-주문알림
    do: 퀵스타 연결
  - id: tc1325_카카오톡-주문알림
    do: 모달 취소 버튼 선택
  - id: tc1327_카카오톡-주문알림
    do: 회원가입 하기 선택
  - id: tc1328_카카오톡-주문알림
    do: 회원가입 모달
  - id: tc1329_카카오톡-주문알림
    do: 회원가입 인풋 채우기

expected: 모든 step 이 성공. spec.ts 구현 후 활성화.
