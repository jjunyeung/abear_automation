# 연결 설정 / 카카오톡 연결 — P1 미작성 TC 4건 (auto-generated skeleton).
#
# 'scripts/generate-all-priority-skeletons.py' 가 자동 생성한 placeholder.
# 각 step = 엑셀 TC 1건. spec.ts 는 아직 없음 → catalog 에서 stale 로 노출됨.
#
# 작업 방법:
#   1. step.id ↔ Playwright 액션 매핑을 tests/base-setting/p1-연결-설정-카카오톡-연결.spec.ts 에 구현
#   2. tests/base-setting/p1-연결-설정-카카오톡-연결.handlerKeys.json 에 step.id 들 등록
#   3. selector 는 windly_repo/* 의 해당 컴포넌트 코드 확인 후 추출

title: 연결 설정 / 카카오톡 연결 영역 P1 TC 묶음 (4건)
priority: P1

description: |
  자동 생성된 영역 단위 skeleton. 엑셀 export 의 P1 미작성 TC 4건을 step 으로 모음.
  spec.ts 구현 전까지 catalog 에서 stale (handlerKeys 매니페스트 없음) 로 표시.

inputs: {}

steps:
  - id: tc3409_func-실제-알림-화면-미리보기-링크
    do: 실제 알림 화면 미리보기 링크 클릭 시, 해당 링크로 이동한다.
  - id: tc3414_func-카카오톡-계정-연결-해제
    do: 연결 해제 버튼 클릭 시, 카카오 계정 연결 해제 모달이 노출된다.
  - id: tc3418_func-카카오-계정-연결-해제-모달-취소
    do: 취소 버튼 클릭 시, 카카오 계정 연결 해제 모달이 닫히고 이전 화면으로 돌아간다.
  - id: tc3422_func-모달-노출
    do: 카카오톡 주문알림 섹션에서 지금 활성화하기 버튼 클릭 시 기능 제한 모달 노출 확인

expected: 모든 step 이 성공. spec.ts 구현 후 활성화.
