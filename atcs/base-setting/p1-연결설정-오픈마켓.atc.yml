# 연결설정 / 오픈마켓 — P1 미작성 TC 3건 (auto-generated skeleton).
#
# 'scripts/generate-all-priority-skeletons.py' 가 자동 생성한 placeholder.
# 각 step = 엑셀 TC 1건. spec.ts 는 아직 없음 → catalog 에서 stale 로 노출됨.
#
# 작업 방법:
#   1. step.id ↔ Playwright 액션 매핑을 tests/base-setting/p1-연결설정-오픈마켓.spec.ts 에 구현
#   2. tests/base-setting/p1-연결설정-오픈마켓.handlerKeys.json 에 step.id 들 등록
#   3. selector 는 windly_repo/* 의 해당 컴포넌트 코드 확인 후 추출

title: 연결설정 / 오픈마켓 영역 P1 TC 묶음 (3건)
priority: P1

description: |
  자동 생성된 영역 단위 skeleton. 엑셀 export 의 P1 미작성 TC 3건을 step 으로 모음.
  spec.ts 구현 전까지 catalog 에서 stale (handlerKeys 매니페스트 없음) 로 표시.

inputs: {}

steps:
  - id: tc991
    do: 고객센터 버튼 선택
  - id: tc992
    do: 사용가이드 선택
  - id: tc1009
    do: 잘못된 인증키 입력 후 연결

expected: 모든 step 이 성공. spec.ts 구현 후 활성화.
