# 상세페이지 / 이미지편집 — P0 미작성 TC 7건 (auto-generated skeleton).
#
# 'scripts/generate-all-priority-skeletons.py' 가 자동 생성한 placeholder.
# 각 step = 엑셀 TC 1건. spec.ts 는 아직 없음 → catalog 에서 stale 로 노출됨.
#
# 작업 방법:
#   1. step.id ↔ Playwright 액션 매핑을 tests/collected-product/p0-상세페이지-이미지편집.spec.ts 에 구현
#   2. tests/collected-product/p0-상세페이지-이미지편집.handlerKeys.json 에 step.id 들 등록
#   3. selector 는 windly_repo/* 의 해당 컴포넌트 코드 확인 후 추출

title: 상세페이지 / 이미지편집 영역 P0 TC 묶음 (7건)
priority: P0

description: |
  자동 생성된 영역 단위 skeleton. 엑셀 export 의 P0 미작성 TC 7건을 step 으로 모음.
  spec.ts 구현 전까지 catalog 에서 stale (handlerKeys 매니페스트 없음) 로 표시.

inputs: {}

steps:
  - id: tc842
    do: 모자이크 버튼 선택
  - id: tc843
    do: 브러시 크기 조절
  - id: tc844
    do: 모자이크 강도 선택
  - id: tc845
    do: 모자이크 실행 버튼 선택
  - id: tc846
    do: 초기화 버튼 선택
  - id: tc847
    do: 이전 버튼 선택
  - id: tc848
    do: 끝내기 버튼 선택

expected: 모든 step 이 성공. spec.ts 구현 후 활성화.
