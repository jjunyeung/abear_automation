# 인기 상품 추천 개선 / 키워드목록 — P0 미작성 TC 3건 (auto-generated skeleton).
#
# 'scripts/generate-all-priority-skeletons.py' 가 자동 생성한 placeholder.
# 각 step = 엑셀 TC 1건. spec.ts 는 아직 없음 → catalog 에서 stale 로 노출됨.
#
# 작업 방법:
#   1. step.id ↔ Playwright 액션 매핑을 tests/collected-product/p0-인기-상품-추천-개선-키워드목록.spec.ts 에 구현
#   2. tests/collected-product/p0-인기-상품-추천-개선-키워드목록.handlerKeys.json 에 step.id 들 등록
#   3. selector 는 windly_repo/* 의 해당 컴포넌트 코드 확인 후 추출

title: 인기 상품 추천 개선 / 키워드목록 영역 P0 TC 묶음 (3건)
priority: P0

description: |
  자동 생성된 영역 단위 skeleton. 엑셀 export 의 P0 미작성 TC 3건을 step 으로 모음.
  spec.ts 구현 전까지 catalog 에서 stale (handlerKeys 매니페스트 없음) 로 표시.

inputs: {}

steps:
  - id: tc3674_키워드-개수
    do: 추천 키워드 20개 노출
  - id: tc3675_키워드-선택-후-상품-조회
    do: 키워드 선택 시 상품 목록 조회
  - id: tc3677_ab-테스트-분기
    do: A/B 테스트 그룹별 키워드 개수 분기 확인

expected: 모든 step 이 성공. spec.ts 구현 후 활성화.
