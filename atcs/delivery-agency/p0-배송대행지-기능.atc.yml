# 배송대행지 / 기능 — P0 미작성 TC 6건 (auto-generated skeleton).
#
# Case No: 1520, 1521, 1522, 1523, 1524, 1525
# Sub-fn: 신청서 리스트 화면 기능 / 검색 → 날짜필터 → 업데이트 → 트래킹번호 → 택배사/송장번호 → 검수 사진 확인.
#
# 이 ATC 는 'scripts/generate-p0-skeletons.py' 가 자동 생성한 placeholder.
# 각 step = 엑셀 TC 1건. spec.ts 는 아직 없음 → catalog 에서 stale 로 노출됨.
#
# 다음 작업 (R9+):
#   1. 진입 URL = app.windly.cc/view3/delivery-agency (신청서 목록 화면).
#   2. windly_repo/sesame-frontend-web 의 신청서 목록 컴포넌트 (검색바, 날짜필터, 업데이트 버튼,
#      트래킹번호 컬럼, 검수사진 모달) selector 추출.
#   3. step.id ↔ Playwright 액션 매핑을 tests/delivery-agency/p0-배송대행지-기능.spec.ts 구현.
#   4. handlerKeys.json 에 6개 step.id 등록.

title: 배송대행지 / 기능 영역 P0 TC 묶음 (6건)
priority: P0

description: |
  자동 생성된 영역 단위 skeleton. 엑셀 export 의 P0 미작성 TC 6건을 step 으로 모음.
  spec.ts 구현 전까지 catalog 에서 stale (handlerKeys 매니페스트 없음) 로 표시.

inputs: {}

steps:
  - id: tc1520
    do: 검색
  - id: tc1521
    do: 날짜필터 선택
  - id: tc1522
    do: 업데이트선택
  - id: tc1523
    do: 트래킹번호 선택
  - id: tc1524
    do: 택배사/송장번호 선택
  - id: tc1525
    do: 검수 사진 확인

expected: 모든 step 이 성공. spec.ts 구현 후 활성화.
