# 스스관부가세 / 공통 — P0 미작성 TC 3건 (auto-generated skeleton).
#
# 이 ATC 는 'scripts/generate-p0-skeletons.py' 가 자동 생성한 placeholder.
# 각 step = 엑셀 TC 1건. spec.ts 는 아직 없음 → catalog 에서 stale 로 노출됨.
#
# 작업 방법:
#   1. step.id ↔ Playwright 액션 매핑을 tests/smartstore-customs-tax/p0-스스관부가세-공통.spec.ts 에 구현
#   2. tests/smartstore-customs-tax/p0-스스관부가세-공통.handlerKeys.json 에 step.id 들 등록
#   3. selector 는 windly_repo/* 의 해당 컴포넌트 코드 확인 후 추출

title: 스스관부가세 / 공통 영역 P0 TC 묶음 (3건)
priority: P0

description: |
  자동 생성된 영역 단위 skeleton. 엑셀 export 의 P0 미작성 TC 3건을 step 으로 모음.
  spec.ts 구현 전까지 catalog 에서 stale (handlerKeys 매니페스트 없음) 로 표시.

inputs: {}

steps:
  - id: tc3532_저장-관련-액션
    do: 수정한 업로드 설정을 저장할 수 있다
  - id: tc3533_저장-관련-액션
    do: 저장한 업로드 설정이 재진입 후 유지된다
  - id: tc3534_필수값-검증
    do: 필수 항목 누락 시 저장이 제한된다

expected: 모든 step 이 성공. spec.ts 구현 후 활성화.
