# 스스관부가세 / 스마트스토어 업로드 설정 — P0 미작성 TC 7건 (auto-generated skeleton).
#
# 이 ATC 는 'scripts/generate-p0-skeletons.py' 가 자동 생성한 placeholder.
# 각 step = 엑셀 TC 1건. spec.ts 는 아직 없음 → catalog 에서 stale 로 노출됨.
#
# 작업 방법:
#   1. step.id ↔ Playwright 액션 매핑을 tests/smartstore-customs-tax/p0-스스관부가세-스마트스토어-업로드-설정.spec.ts 에 구현
#   2. tests/smartstore-customs-tax/p0-스스관부가세-스마트스토어-업로드-설정.handlerKeys.json 에 step.id 들 등록
#   3. selector 는 windly_repo/* 의 해당 컴포넌트 코드 확인 후 추출

title: 스스관부가세 / 스마트스토어 업로드 설정 영역 P0 TC 묶음 (7건)
priority: P0

description: |
  자동 생성된 영역 단위 skeleton. 엑셀 export 의 P0 미작성 TC 7건을 step 으로 모음.
  spec.ts 구현 전까지 catalog 에서 stale (handlerKeys 매니페스트 없음) 로 표시.

inputs: {}

steps:
  - id: tc3494_화면-진입
    do: 스마트스토어 업로드 설정 화면이 정상 진입된다
  - id: tc3499_업로드-대상-마켓
    do: 업로드 대상 마켓 토글이 정상 노출된다
  - id: tc3500_업로드-대상-마켓
    do: 업로드 대상 마켓은 개별 선택 상태를 표시한다
  - id: tc3501_업로드-대상-마켓
    do: 업로드 대상 마켓 토글을 변경할 수 있다
  - id: tc3515_관부가세-설정
    do: 관부가세 설정 영역이 정상 노출된다
  - id: tc3516_관부가세-설정
    do: 관부가세 옵션 3개가 모두 노출된다
  - id: tc3517_관부가세-설정
    do: 관부가세 옵션은 단일 선택으로 동작한다

expected: 모든 step 이 성공. spec.ts 구현 후 활성화.
