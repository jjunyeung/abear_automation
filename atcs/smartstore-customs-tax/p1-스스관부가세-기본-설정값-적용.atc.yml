# 스스관부가세 / 기본 설정값 적용 — P1 미작성 TC 7건 (auto-generated skeleton).
#
# 'scripts/generate-all-priority-skeletons.py' 가 자동 생성한 placeholder.
# 각 step = 엑셀 TC 1건. spec.ts 는 아직 없음 → catalog 에서 stale 로 노출됨.
#
# 작업 방법:
#   1. step.id ↔ Playwright 액션 매핑을 tests/smartstore-customs-tax/p1-스스관부가세-기본-설정값-적용.spec.ts 에 구현
#   2. tests/smartstore-customs-tax/p1-스스관부가세-기본-설정값-적용.handlerKeys.json 에 step.id 들 등록
#   3. selector 는 windly_repo/* 의 해당 컴포넌트 코드 확인 후 추출

title: 스스관부가세 / 기본 설정값 적용 영역 P1 TC 묶음 (7건)
priority: P1

description: |
  자동 생성된 영역 단위 skeleton. 엑셀 export 의 P1 미작성 TC 7건을 step 으로 모음.
  spec.ts 구현 전까지 catalog 에서 stale (handlerKeys 매니페스트 없음) 로 표시.

inputs: {}

steps:
  - id: tc3550_모달-항목-요약
    do: 기본 설정값 적용 항목 요약 영역이 정상 노출된다
  - id: tc3556_모달-툴팁-아이콘
    do: 항목별 툴팁 아이콘이 정상 노출된다
  - id: tc3557_판매가-툴팁
    do: 판매가 툴팁 상세 문구가 정상 노출된다
  - id: tc3558_배송-툴팁
    do: 배송 툴팁 상세 문구가 정상 노출된다
  - id: tc3559_상세페이지-설정-툴팁
    do: 상세페이지 설정 툴팁 상세 문구가 정상 노출된다
  - id: tc3560_리뷰-포인트-툴팁
    do: 리뷰 포인트 툴팁 상세 문구가 정상 노출된다
  - id: tc3564_되돌리기-안내-문구
    do: 되돌릴 수 없음 안내 문구가 사용자에게 명확히 전달된다

expected: 모든 step 이 성공. spec.ts 구현 후 활성화.
