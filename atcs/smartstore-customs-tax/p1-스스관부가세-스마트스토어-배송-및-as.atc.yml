# 스스관부가세 / 스마트스토어 배송 및 A/S — P1 미작성 TC 9건 (auto-generated skeleton).
#
# 'scripts/generate-all-priority-skeletons.py' 가 자동 생성한 placeholder.
# 각 step = 엑셀 TC 1건. spec.ts 는 아직 없음 → catalog 에서 stale 로 노출됨.
#
# 작업 방법:
#   1. step.id ↔ Playwright 액션 매핑을 tests/smartstore-customs-tax/p1-스스관부가세-스마트스토어-배송-및-as.spec.ts 에 구현
#   2. tests/smartstore-customs-tax/p1-스스관부가세-스마트스토어-배송-및-as.handlerKeys.json 에 step.id 들 등록
#   3. selector 는 windly_repo/* 의 해당 컴포넌트 코드 확인 후 추출

title: 스스관부가세 / 스마트스토어 배송 및 A/S 영역 P1 TC 묶음 (9건)
priority: P1

description: |
  자동 생성된 영역 단위 skeleton. 엑셀 export 의 P1 미작성 TC 9건을 step 으로 모음.
  spec.ts 구현 전까지 catalog 에서 stale (handlerKeys 매니페스트 없음) 로 표시.

inputs: {}

steps:
  - id: tc3468_화면-기본-ui
    do: 배송 및 A/S 화면의 기본 UI가 정상 노출된다
  - id: tc3477_추가-배송비-설정
    do: 제주 지역 추가 배송비 입력 필드가 노출된다
  - id: tc3478_추가-배송비-설정
    do: 제주 외 도서산간 추가 배송비 입력 필드가 노출된다
  - id: tc3479_추가-배송비-설정
    do: 추가 배송비 기본값이 정상 표시된다
  - id: tc3480_추가-배송비-설정
    do: 추가 배송비에는 숫자만 입력할 수 있다
  - id: tc3485_관부가세-설정
    do: 저장된 관부가세 값이 기본 선택 상태로 표시된다
  - id: tc3486_as-정보
    do: A/S 정보 섹션이 정상 노출된다
  - id: tc3489_as-안내내용
    do: A/S 안내 내용 입력 필드가 정상 노출된다
  - id: tc3490_as-안내내용
    do: A/S 안내 내용을 입력할 수 있다

expected: 모든 step 이 성공. spec.ts 구현 후 활성화.
