# 스스관부가세 / 쿠팡 업로드 설정 — P1 미작성 TC 9건 (auto-generated skeleton).
#
# 'scripts/generate-all-priority-skeletons.py' 가 자동 생성한 placeholder.
# 각 step = 엑셀 TC 1건. spec.ts 는 아직 없음 → catalog 에서 stale 로 노출됨.
#
# 작업 방법:
#   1. step.id ↔ Playwright 액션 매핑을 tests/smartstore-customs-tax/p1-스스관부가세-쿠팡-업로드-설정.spec.ts 에 구현
#   2. tests/smartstore-customs-tax/p1-스스관부가세-쿠팡-업로드-설정.handlerKeys.json 에 step.id 들 등록
#   3. selector 는 windly_repo/* 의 해당 컴포넌트 코드 확인 후 추출

title: 스스관부가세 / 쿠팡 업로드 설정 영역 P1 TC 묶음 (9건)
priority: P1

description: |
  자동 생성된 영역 단위 skeleton. 엑셀 export 의 P1 미작성 TC 9건을 step 으로 모음.
  spec.ts 구현 전까지 catalog 에서 stale (handlerKeys 매니페스트 없음) 로 표시.

inputs: {}

steps:
  - id: tc3519_섹션-노출
    do: 쿠팡 업로드 설정 섹션이 정상 노출된다
  - id: tc3522_검색-필터
    do: 검색 필터 섹션이 정상 노출된다
  - id: tc3523_검색-필터
    do: 검색 필터 액션 버튼이 정상 노출된다
  - id: tc3524_검색-필터
    do: 검색 필터 입력 항목이 정상 노출된다
  - id: tc3525_검색-필터
    do: 검색 필터 입력 필드에 값을 입력할 수 있다
  - id: tc3526_검색-필터
    do: 전체 복사 버튼이 정상 동작한다
  - id: tc3527_검색-필터
    do: 붙여넣기 버튼이 정상 동작한다
  - id: tc3528_검색-필터
    do: 자동 입력 버튼이 정상 동작한다
  - id: tc3529_검색-필터
    do: 전체 삭제 버튼이 정상 동작한다

expected: 모든 step 이 성공. spec.ts 구현 후 활성화.
