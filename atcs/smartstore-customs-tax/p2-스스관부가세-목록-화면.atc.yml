# 스스관부가세 / 목록 화면 — P2 미작성 TC 2건 (auto-generated skeleton).
#
# 'scripts/generate-all-priority-skeletons.py' 가 자동 생성한 placeholder.
# 각 step = 엑셀 TC 1건. spec.ts 는 아직 없음 → catalog 에서 stale 로 노출됨.
#
# 작업 방법:
#   1. step.id ↔ Playwright 액션 매핑을 tests/smartstore-customs-tax/p2-스스관부가세-목록-화면.spec.ts 에 구현
#   2. tests/smartstore-customs-tax/p2-스스관부가세-목록-화면.handlerKeys.json 에 step.id 들 등록
#   3. selector 는 windly_repo/* 의 해당 컴포넌트 코드 확인 후 추출

title: 스스관부가세 / 목록 화면 영역 P2 TC 묶음 (2건)
priority: P2

description: |
  자동 생성된 영역 단위 skeleton. 엑셀 export 의 P2 미작성 TC 2건을 step 으로 모음.
  spec.ts 구현 전까지 catalog 에서 stale (handlerKeys 매니페스트 없음) 로 표시.

inputs: {}

steps:
  - id: tc3538_보기-전환
    do: 카드뷰와 목록뷰 전환 버튼이 정상 노출된다
  - id: tc3539_정렬페이지-수
    do: 정렬 및 페이지 수 설정 영역이 정상 노출된다

expected: 모든 step 이 성공. spec.ts 구현 후 활성화.
