# 유입수 분석 / 등록상품 상세 — P0 미작성 TC 4건 (auto-generated skeleton).
#
# 'scripts/generate-all-priority-skeletons.py' 가 자동 생성한 placeholder.
# 각 step = 엑셀 TC 1건. spec.ts 는 아직 없음 → catalog 에서 stale 로 노출됨.
#
# 작업 방법:
#   1. step.id ↔ Playwright 액션 매핑을 tests/windly-shell/p0-유입수-분석-등록상품-상세.spec.ts 에 구현
#   2. tests/windly-shell/p0-유입수-분석-등록상품-상세.handlerKeys.json 에 step.id 들 등록
#   3. selector 는 windly_repo/* 의 해당 컴포넌트 코드 확인 후 추출

title: 유입수 분석 / 등록상품 상세 영역 P0 TC 묶음 (4건)
priority: P0

description: |
  자동 생성된 영역 단위 skeleton. 엑셀 export 의 P0 미작성 TC 4건을 step 으로 모음.
  spec.ts 구현 전까지 catalog 에서 stale (handlerKeys 매니페스트 없음) 로 표시.

inputs: {}

steps:
  - id: tc1115_수정-업로드
    do: 톡스토어 유입수 분석
  - id: tc1116_수정-업로드
    do: 톡스토어 그래프 확인
  - id: tc1117_수정-업로드
    do: 상품 리스트 확인
  - id: tc1118_수정-업로드
    do: 정렬

expected: 모든 step 이 성공. spec.ts 구현 후 활성화.
