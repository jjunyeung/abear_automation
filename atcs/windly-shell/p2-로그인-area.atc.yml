# 로그인 /  — P2 미작성 TC 11건 (auto-generated skeleton).
#
# 'scripts/generate-all-priority-skeletons.py' 가 자동 생성한 placeholder.
# 각 step = 엑셀 TC 1건. spec.ts 는 아직 없음 → catalog 에서 stale 로 노출됨.
#
# 작업 방법:
#   1. step.id ↔ Playwright 액션 매핑을 tests/windly-shell/p2-로그인-area.spec.ts 에 구현
#   2. tests/windly-shell/p2-로그인-area.handlerKeys.json 에 step.id 들 등록
#   3. selector 는 windly_repo/* 의 해당 컴포넌트 코드 확인 후 추출

title: 로그인 /  영역 P2 TC 묶음 (11건)
priority: P2

description: |
  자동 생성된 영역 단위 skeleton. 엑셀 export 의 P2 미작성 TC 11건을 step 으로 모음.
  spec.ts 구현 전까지 catalog 에서 stale (handlerKeys 매니페스트 없음) 로 표시.

inputs: {}

steps:
  - id: tc6
    do: 이메일 로그인
  - id: tc7
    do: 이메일 로그인
  - id: tc8
    do: 네이버 로그인
  - id: tc9
    do: 네이버 로그인
  - id: tc10
    do: 네이버 로그인
  - id: tc11
    do: 구글 로그인
  - id: tc12
    do: 구글 로그인
  - id: tc13
    do: 구글 로그인
  - id: tc14
    do: 회원가입 실패
  - id: tc15
    do: 네이버 회원가입 시도
  - id: tc16
    do: 구글 회원가입 시도

expected: 모든 step 이 성공. spec.ts 구현 후 활성화.
